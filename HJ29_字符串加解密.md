描述
1、对输入的字符串进行加解密，并输出。

2、加密方法为：

当内容是英文字母时则用该英文字母的后一个字母替换，同时字母变换大小写,如字母a时则替换为B；字母Z时则替换为a；

当内容是数字时则把该数字加1，如0替换1，1替换2，9替换0；

其他字符不做变化。

3、解密方法为加密的逆过程。


本题含有多组样例输入。
输入描述：
输入说明
输入一串要加密的密码
输入一串加过密的密码

输出描述：
输出说明
输出加密后的字符
输出解密后的字符

示例1
输入：
    abcdefg
    BCDEFGH
输出：
    BCDEFGH
    abcdefg

```python
# coding=utf8
from string import ascii_lowercase
while 1:
    try:
        str_encode = input()
        str_decode = input()

        s_encode = ''
        for w in str_encode:

            if w.isalpha():
                if w == 'Z':
                    s_encode += 'a'
                elif w == 'z':
                    s_encode += 'A'
                elif w.isupper():
                    s_encode += chr(ord(w)+1).lower()
                elif w.islower():
                    s_encode += chr(ord(w)+1).upper()
                else:
                    s_encode += '#'
            elif w.isnumeric():
                if w == '9':
                    s_encode += '0'
                else:
                    s_encode += str(int(w)+1)
            else:
                s_encode += '#'
        # decode
        s_decode = ''
        for w in str_decode:
            if w.isalpha():
                if w == 'a':
                    s_decode += 'Z'
                elif w == 'A':
                    s_decode += 'z'
                elif w.isupper():
                    s_decode += chr(ord(w.lower()) - 1)
                elif w.islower():
                    s_decode += chr(ord(w.upper()) - 1)
                else:
                    s_decode += '#'
            else:
                if w == '0':
                    s_decode += '9'
                else:
                    s_decode += str(int(w)-1)
        print(s_encode)
        print(s_decode)
    except:
        break
```


```php
<?php

  while($str_1=trim(fgets(STDIN))){
      # 加密
      $len = strlen($str_1);
      $out_encode = '';
      for($i=0;$i<$len;$i++){
          if(ctype_alpha($str_1[$i])){

              if($str_1[$i]=='Z'){
                  $out .= 'a';
              }elseif($str_1[$i]=='z'){
                  $out .= 'A';
              }elseif(ctype_lower($str_1[$i])){
                  $out .= strtoupper(chr(ord($str_1[$i])+1));
              }elseif(ctype_upper($str_1[$i])){
                  $out .= strtolower(chr(ord($str_1[$i])+1));
              }else{
                  $out .= '#';
              }
          }else{
              if($str_1[$i] == '9'){
                  $out .= '0';
              }else{
                  $out .= (string)(intval($str_1[$i]) + 1);
               }
          }
      }
      # 解密
      $str_2 = trim(fgets(STDIN));
      $len = strlen($str_2);
      $out_2 = '';

      for($i=0;$i<$len;$i++){
          if(ctype_alpha($str_2[$i])){
              if($str_2[$i]=='a'){
                  $out_2 .= 'Z';
              }elseif($str_2[$i]=='A'){
                  $out_2 .= 'z';
              }elseif(ctype_lower($str_2[$i])){
                  $out_2 .= strtoupper(chr(ord($str_2[$i])-1));
              }elseif(ctype_upper($str_2[$i])){
                  $out_2 .= strtolower(chr(ord($str_2[$i])-1));
              }else{
                  $out_2 .= '#';
              }
          }else{
              if($str_2[$i] == '0'){
                  $out_2 .= '9';
              }else{
                  $out_2 .= (string)(intval($str_2[$i]) - 1);
               }
          }
      }

      echo $out,PHP_EOL;
      echo $out_2,PHP_EOL;
      unset($str_1, $str_2, $out, $out_2);
  }
```
